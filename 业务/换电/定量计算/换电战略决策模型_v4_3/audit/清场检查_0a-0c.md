# 清场检查报告：0a / 0b / 0c

> ⚠️ **2026-09-07 补记**：`交接.md` 第五节已整节重写为新路线，本报告当初对应的「§5.0 清场」任务清单**已不存在**。
> **本报告的三项检查结论仍然有效**（0a 覆盖检查、0b `templates/` 仍在被引用、0c 旧叙述稿判定），
> 已执行的搬迁也已完成；**未执行的归档动作现已挂起**，条件与归宿见 `交接.md` §4.3、§5.5。
> 文中提到的「第 8 步（装配）」对应新的 `框架提案.md` §4.3 第 8 项。
>
> 2026-09-06 · 对应当时 `交接.md` §5.0 的三项 AI 侧检查。
> **0d（重跑）与 0e（`git mv`）仍归你**，本报告只提供执行依据。
> 结论汇总见末尾《你要执行的动作》。

---

## 0a · `MANIFEST.md` 覆盖检查

**方法**：逐节问一句——**这一节的职责，现在归谁？** 三种结果：已被接管／降级为素材／没人接。

| MANIFEST 节 | 职责现在归谁 | 判定 |
|---|---|---|
| 快速导读（一分钟版） | `README.md` 导航段 | **已接管** |
| 0 本文档体系与阅读方式 | `README.md` ＋ `研究项目约定.md` §2（目录准入判据） | **已接管**（而且接得更好——准入判据是可执行的，模型地图只是描述） |
| 1 决策问题与模型总架构 | `框架提案.md` §2 流水线五层 ＋ `交接.md` 二 分层结构 | **已接管** |
| 2 车辆层：四类车 → 终局参数契约 | 参数 → `configs/base.toml`；算法 → `src/scale.py`；**论证 → `topics/目标用户_谁的痛点.md`（本轮新建）** | **已接管** |
| 3 站点层：站数反推与装机规模 | 同上；"站数一律由需求反推"这条口径已在 `交接.md` 已定决策 | **已接管** |
| 4 运营层：换电量、收入与成本 | `src/business.py` ＋ `base.toml` | **已接管** |
| 5 电池层：寿命、重置与价格曲线 | `src/capex.py` ＋ `口径/capex_debt_估值公式链.md` | **已接管** |
| 6 财务与估值层 | `口径/capex_debt_估值公式链.md`（两本账、三个资本数）＋ `框架提案.md` §1.5 | **已接管** |
| 7 敏感性结论：谁在撬动这个模型 | `src/lab.py scan` → `outputs/敏感性矩阵_v4.3.csv`；报告侧是第 7B 章 | **已接管**（且从静态文字变成了可重跑的产物） |
| 8 集团资金约束与并购情景 | 资金门 → `src/decision.py` ＋ 第 3B 章 | **已接管** |
| ├ **§8.2 参照公司卡片** | — | **⚠️ 降级为素材，见下** |
| 9 历史演进链 | `DECISIONS.md` | **已接管**（DECISIONS 更细、可检索、带类型） |
| 10 信源总索引 | `audit/信源审计台账.md` | **已接管** |
| 附 结构稿 3 个决策点确认记录 | `DECISIONS.md` | **已接管** |

### ⚠️ 唯一需要单独处理的：§8.2 参照公司卡片

`交接.md` 与 `框架提案.md` 里"**禁止引用启源芯动力交易做倍数依据**"这条决策，
引述的出处正是 `MANIFEST.md §8.2`。归档后这个指针会指向 `_archive/`。

**判定：可以接受，不阻塞归档。** 理由：

- **决策本身是自足的**——理由（CATL 对国资的并购有多种考量，不是市场化定价）
  已完整写在 `交接.md` 已定决策里，不依赖 §8.2 的原文
- §8.2 的功能是**证据留痕**，不是论证；证据留在 `_archive/` 里可查即可

**动作**：归档后把两处引用改成 `_archive/MANIFEST.md §8.2`。本报告已列明，改动很小。

### ⚠️ 0a 结论已修正（2026-09-06）

**上面那张表里，把 §2–§6 判成"归 `src/*.py` ＋ `base.toml`"是错的。**

> **"有程序实现" ≠ "有人接管"。** 程序接管的是**执行**，不是**表达**——**读者不可能去读代码。**
> `src/` 里是算法，`base.toml` 里是参数和一行理由，**"为什么这么推"那段人话没有 home。**
>
> **判据：把这段话删掉，还有没有人能从别处读到同样的意思？读代码不算。**

而 `MANIFEST.md` §0.1 自己就写清了定位——
**"唯一事实源：决策逻辑、取数论证、计算链、演进历史、信源｜读者：人，一次读完全懂"**。
**新报告取代的正是这个位置**，所以必须逐段接管，不能靠"程序里有"打发。

**修正后的接管方案：**

| MANIFEST 段 | 接管到哪 | 状态 |
|---|---|---|
| §1.0–1.2 决策问题、情景体系 | 第 0 章 ＋ `框架提案.md` §2 | 已接管 |
| **§2 车辆层**（四类车四种推算法）＋ **§3 站点层**（站数反推、单站接待能力物理上限） | **`口径/车辆与站数_推算方法.md`** | **待建** |
| **§4 运营层**（收入/成本计算式、三路径核验；§4.2 是作者自标的样板章节） | **`口径/运营收入与成本_口径.md`** | **待建** |
| §5 电池层（寿命四池、折扣链、重置成本） | `口径/capex_debt_估值公式链.md`，**补寿命模型那段** | 部分接管 |
| **§6.2/6.3 换电对制造业务的影响、总贡献、两把标尺** | **`topics/增量价值_运营单体加制造影响.src.md`** ＋ 第 4 章 | **✅ 本轮已建** |
| §7 敏感性 | `outputs/敏感性矩阵` ＋ 第 7B 章 | 已接管（且从静态文字变成可重跑） |
| **§8.3 并购四情景** | **`topics/并购_节奏与竞合.src.md`** ＋ 第 3B/6 章 | **✅ 本轮已建** |
| §9 演进链 / §10 信源索引 | `DECISIONS.md` / `audit/信源审计台账.md` | 已接管 |

**所以 `MANIFEST.md` 的归档要等两份口径文档建完**——
本轮已完成 §6 与 §8.3 的接管，**§2/§3/§4 仍在它身上**。

### 0a 原结论

> **没有一节是"没人接"的。** `MANIFEST.md` 可以归档。
> 值得记一句：它的问题从来不是内容错，是**一个文件同时服务三类读者**
> （论点／口径／链路），拆开之后每一块都比原来清楚——**这正是三分法要解决的病。**

---

## 0b · `templates/` 引用检查

**方法**：`grep -rn "template|\.tpl" src/*.py`

```
src/report.py:2635:    template_path = ROOT.parent / "templates" / "strategic_report_v4_3.md.tpl"
src/report.py:2636:    return Template(template_path.read_text(encoding="utf-8")).substitute(values)
```

### 0b 结论：**仍在被引用，不能归档。**

`src/report.py` 用它生成 `outputs/换电战略决策报告_v4.3.md`。
`交接.md` §5.0 原本假设"若 `src/` 已不引用 → `_archive/`"——**前提不成立。**

**改成什么**：`templates/` **保留**，并补一条准入判据（本轮已写进 `研究项目约定.md` §2.2）：

> `templates/` — 是**输出格式模板**，被 `src/` 引用；**不含任何业务数字与口径判断**
> （判据同 HTML 边界：把模板删掉重写，报告结论一个字不变）。

`交接.md` 七·补的目标结构图已同步补上这一行。

---

## 0c · `narrative/` 旧稿判定

**文件**：`narrative/换电投资逻辑_叙述报告_v4.3.src.md`（12.2 KB）

**它的结构**：

```
一、先认识主角和这门生意     五、Q3 换回什么：价值从哪来
二、决策逻辑：为什么必须做   六、Q4 钱从哪来
三、Q1 做多大               七、这套测算可能错在哪（诚实清单）
四、Q2 花多少钱             八、一句话结论
```

**对照新八章骨架**（`框架提案.md` §1.1）：0 决策卡／1 非共识／2 必要性／3A 谁是真用户／
3B 四道门／4 业绩影响／5 市值与重估／6 五年判断／7A+7B 诚实清单。

### 0c 结论：**是旧结构，判定归档。**

它是 **Q1–Q4 的模型说明书骨架**，不是**按决策问题走的报告骨架**——
正是 `框架提案.md` §1.1 那句"骨架不按模型的层走，按决策的问题走"要替换掉的东西。

**但有两处不是废物，归档前先摘出来**（本轮已摘）：

| 旧稿的东西 | 去哪 |
|---|---|
| 「先认识主角和这门生意」那种**面向不熟悉 CATL 的读者**的讲法 | 新第 0 章决策卡的语气参照——**旧稿在"讲明白"上比新骨架强**，别丢 |
| 第七节「这套测算可能错在哪」 | 新 7A/7B 的前身；7A 现在改为**从 `DECISIONS.md` 生成**，旧稿可作对照 |

### ⚠️ 0c 连带发现（**阻塞项，本轮已修**）

`src/inject.py` 第 186 行原本是：

```python
sources = sorted(NARRATIVE_DIR.glob("*.src.md"))     # 只扫 narrative/ 顶层
```

搬迁后章节住在 `narrative/chapters/`、专题住在 `narrative/topics/`，
**这个 glob 会一个文件都扫不到，而且不会报错——它会打印"没有 .src.md，跳过叙述层"然后正常退出。**
**一次静默失效，比崩溃难发现。**

**已改为 `rglob`**（递归），并在代码里留了注释指向本报告。

---

## ⚠️ 三项检查之外，发现两个真实缺口

### 缺口一：`{{topic: ...}}` 引用机制不存在

`框架提案.md` §1.4 写了"章里用 `{{topic: 业务本质_第一性原理拆解}}` 引用，
`build.py` 装配时展开"。但 `inject.py` 的 `TOKEN_RE` 是：

```python
re.compile(r"\{\{\s*([A-Za-z0-9_.]+)\s*(?::\s*([a-z]+)\s*)?\}\}")
```

**只认 ASCII 键名，且冒号后面是格式说明符（`:n`），不是参数。**
所以 `{{topic: 中文名}}` 既匹配不上，也不会被展开。

**判定：不阻塞本轮，归入第 8 步（装配）。** 现在 topics 各自独立成文、章里用普通链接引用即可。
**已写进 `交接.md` 4.3 未做。** 不在这一轮实现——它属于装配层，现在做是提前设计。

### 缺口二：叙述层需要的数，有一部分不在 `facts.json` 里

`facts.json` 装的是**模型算出来的数**（96 个键，`base.*` / `q1–q4.*` / `dcf.*` / `sens.*`）。
但专题里要用的还有一类：**外部引用事实**——

| 例 | 出处 |
|---|---|
| 重资产基建族 EV/EBITDA 区间、资管平台族区间、特斯拉倍数 | `v3.2 §4.1`（2026-08-26，Multiples.vc LTM） |
| 电池占整车成本比例、单车年增收、BaaS 购车成本降幅 | JPM《重卡电动化》§8.3、Table 3 |
| 电池包价格、锂价 | TrendForce 2026-08 |

**它们不是模型输出，`facts.py` 不会生成；但叙述层的裸数字纪律同样管着它们。**

**改成什么**：`facts.json` 增设 **`ext.*` 命名空间**——外部引用事实，每条必须带
`from`（信源 URL 或文件§号）与 `as_of`（抓取日期）。
理由同「2026-09-05i」TCO 引用陷阱：**引用一个时点数，必须写清它是哪个时点的。**

**本轮的处理**：新建的专题里，外部数一律写成 `{{ext.xxx}}` 占位符，
**并在下面列出待补清单**——这样专题从第一天起就是合规的，缺的是 `facts.py` 侧一个新命名空间。
**已写进 `交接.md` 4.3 未做。**

#### `ext.*` 待补清单（信源已还原为外部原始出处）

> ⚠️ **一条硬规矩（2026-09-06 补）**：`ext.*` 每条的 `from` **必须是外部可达的信源，
> 不能是本仓库的老版本文档**。老版本只能作为"我在哪儿看到的"线索，**不能作为信源本身**。
> 理由：老版本会被归档，指针会断；而且本项目自己的规矩是
> **"自成体系，不依赖任何老文档即可独立阅读"**。
> 上一版这张表里有四条写的是 `v3.2 §4.1`，已全部还原成原始 URL。

**估值倍数类**（口径均为 LTM；`as_of` 一律记 **2026-08-26**，即在 `v3.2` 里被抓取的日期）

| 键 | 值 | 外部信源 |
|---|---|---|
| `ext.mult_infra_low` / `ext.mult_infra_high` | 重资产基建族区间下沿／上沿 | https://multiples.vc/public-comps/brookfield-infrastructure-valuation-multiples |
| `ext.mult_gp_low` / `ext.mult_gp_high` | 轻资产资管族区间下沿／上沿 | https://valueinvesting.io/BX/valuation/ev_ebitda-multiples |
| `ext.mult_bx` | 该族龙头 | 同上 |
| `ext.mult_tsla` | 车＋能源复合族参照 | https://multiples.vc/public-comps/tesla-valuation-multiples |
| `ext.mult_catl_self` / `ext.mult_byd` | CATL 自身／比亚迪 | https://multiples.vc/public-comps/catl-valuation-multiples |
| ~~`ext.mult_ares`~~ | — | **删除**。`v3.2` 未给独立 URL，且它不承重（族区间靠龙头与族中位撑）。**要用须单独取一次** |

**结构参数类**

| 键 | 值 | 外部信源 |
|---|---|---|
| `ext.catl_equity_share_blended` | CATL 在换电资产上的综合自有出资比例 | **不是外部数，是本模型的派生量**（债/权益结构 × CATL 权益占比）→ **不进 `ext.*`，走 `facts.py` 正常派生** |

**TCO 类**（信源统一为 `业务/换电/media/JPM_重卡电动化_英中对照.md`，该文件是外部报告原文的中译对照）

| 键 | 出处 |
|---|---|
| `ext.batt_share_of_truck` / `ext.pack_400kwh_price` | JPM §8.3 |
| `ext.revenue_uplift_per_truck` / `ext.baas_capex_cut` | JPM §8.3 |
| `ext.payload_loss_ev` / `ext.payload_loss_lng` | JPM Table 3 |
| `ext.tco_ev_vs_lng_3y` / `ext.tco_ev_vs_lng_5y` / `ext.tco_ev_vs_diesel_5y` | JPM Table 3 |
| `ext.lng_price_rise` | JPM 正文（LNG 价格年度涨幅） |

**竞争格局类**——**暂不进 `ext.*`。**
六份旧稿之间存在大量互相打架与无信源的数字，
**逐条登记在 `audit/竞争格局_信源与矛盾清单.md`**。
该清单第五节列出的才是"带原始外部信源、可直接进 `ext.*`"的部分；
第六节那批**无信源的数字不得进叙述层**，要用须自行补证。

**资本闭环类**（`资本闭环_REITs与轻资产化` 用到的外部锚）

| 键 | 值 | 外部信源 |
|---|---|---|
| `ext.reit_sponsor_hold_cn` | 中国公募 REITs 原始权益人强制自持下限 | https://www.21jingji.com/article/20230526/eb1328aa2152117229140e3118a89904.html |
| `ext.reit_sponsor_hold_sg` | 新加坡发起人自持区间 | https://propertyatlas.sg/news/cli-1q-fy2026-asset-light-threshold-crossed-at-59-41-mix-fee/ |
| `ext.mgmt_fee_bps` | 轻资产资管全口径管理费率锚 | 同上 |
| `ext.cap_rate_property` / `ext.cap_rate_concession` | 产权类 REITs 分派率／能源经营权类区间 | 经济参考报（产权类分派率）；investors.com（经营权类） |


---

## 你要执行的动作

```bash
cd 换电战略决策模型_v4_3/

mkdir -p 口径
git mv capex_debt_估值公式链.md 口径/
git mv MANIFEST_original.md _archive/
# git mv MANIFEST.md _archive/     ← 【暂缓】0a 结论已修正：§2/§3/§4 的描述性逻辑还没有接管方
#                                    等 口径/车辆与站数_推算方法.md 与 口径/运营收入与成本_口径.md 建完再搬
git mv narrative/换电投资逻辑_叙述报告_v4.3.src.md _archive/       # 0c 已确认
# templates/ 不动 —— 0b 查出 src/report.py 仍在引用

grep -rn "capex_debt_估值公式链" --include="*.md" .   # 检查残留旧路径
git add . && git commit -m "清场：MANIFEST 与旧叙述稿归档、口径目录建立 · 见 DECISIONS 2026-09-06g"
```

**然后 0d**：`python src/build.py`，把"待复核"清单贴回来。
（`inject.py` 已改 `rglob`，新建的 `narrative/topics/` 会被扫到；
因为里面有未定义的 `{{ext.*}}` 占位符，**这一跑预期会报"未知占位符"——那是对的**，
它正好是 `ext.*` 待补清单的自动校验。）

**不需要你做的**：`narrative/chapters/`、`narrative/topics/` 与本报告已由 AI 直接写入。
