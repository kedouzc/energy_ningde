# 交接：换电投资报告自动化

> 最后更新 **2026-09-06**（第二次·收尾）。**换新对话时，把这一份连同 [README.md](./README.md) 一起给对方读，就能接上。**
> 状态住在仓库里，不住在聊天记录里——这份文件本身就是那条纪律的一部分。
>
> **本文件是「状态」层**：只写"现在是什么"，可被覆盖重写。
> "为什么变成这样"全部住在 [DECISIONS.md](./DECISIONS.md)（过程层，只追加）。
> 两者分工见第七节。

---

## 一、背景：这件事从哪来，要到哪去

### 1.1 目标

让 `换电战略决策模型_v4_3` **自动**产出一份投资人视角的换电分析报告：
**定量算完 → 触发 LLM 写正文 → 装配成一份可下钻、可交互的长报告。**
零件基本都造好了，主要工作在**接缝**。

报告要回答的是一个投资判断，不是模型说明书：
**换电的必要性、可行性、对 CATL 业绩与市值的影响、是否引发价值重估**，
据此判断这是不是一笔值得做的 **5 年期投资**。
投资主题是 **"非共识投资机会"**。

### 1.2 五份历史参考文件（都是同一件事的不同尝试，各有取舍）

| # | 文件 | 长处 | 短处 / 定位 |
|---|---|---|---|
| 1 | `业务/换电/分析结论/CATL换电业务_重构版.md` | 定性强 | 定量只是结尾一章的附属 |
| 2 | `报告/宁德时代_V3.0.html` | 定性强、有指标库 | 指标库未接模型；定量是附录 |
| 3 | `业务/换电/分析结论/换电毛估估_折扣链与总表_v3.2.md` | 定量细、信源全（§4.1 倍数论证、§1.2.2 CRF 依据都在这） | **部分逻辑已过时，与 4 冲突时以 4 为准**；定性回答的是"为什么这么算"而不是"这值不值得买" |
| 4 | `换电战略决策模型_v4_3/MANIFEST.md` | 骨架完整 | AI 协作生成，**语言僵化、机器感重、看不懂、调起来费劲** |
| 5 | `定量计算/model_html/model.html`（及 `model_v1.html`） | **交互设计最好的一份**——"第一步敏感性分析／第二步自动优化"，先给目标再问要调什么 | 单站模型被定死、逐年重复；引擎与 Python 主链会漂移 |

**新报告的要求：定性要好于 1/2/3；定量逻辑要比 3/4 更清晰简洁；
交互要用上 5 那种"勾选调整项 → 实时看结果变化"的方式。**

### 1.3 原始困惑（这些都已在本轮解决，结论见 DECISIONS.md）

| 困惑 | 现在的答案 | 出处 |
|---|---|---|
| MANIFEST 太僵化机器感，看不懂、难调 | 因为**一个文件同时服务三类读者**（论点／口径／链路），拆开就好了 | DECISIONS「文件治理」 |
| `换电战略决策报告_v4.3.md` 割裂、与最终报告逻辑不连贯 | 它是**产物**，不该当成状态维护；正文改 `narrative/*.src.md` | 同上 |
| 想"以终为始"从结果倒推参数（`chain_table.py`） | 已由 `src/tree.py` 取代并跑通 | 第三节 |
| **模型到底要毛估估还是精算？到底该算什么？** | **精度 ≠ 假设数量；让程序算细、报告读粗；标定一次用很多次** | DECISIONS「方法论主线」**（最重要的一条）** |
| DCF 是不是把复杂度加成了"精确的错误" | 不是。但它照出了原本被 EV/EBITDA 盖住的折旧 bug | DECISIONS 09-02 / 09-02c |
| tree.py 看不出谁在区分情景 | 因为**基本没有谁在区分**——base.toml 设计问题 | DECISIONS「三情景名不副实」 |
| DCF 内在价值远低于倍数法，是不是豪赌 | 是赌，但可以拆成四桶；只有第四桶才是真赌 | DECISIONS「估值」 |
| 文件山、版本混乱、思路接不上 | 状态／过程／产物三分 ＋ 单一 git 仓库 | DECISIONS「文件治理」 |
| 和 AI 协作看不到改了哪几行 | 协作协议：**diff 先贴对话、review 通过才落盘** | 第六节 |

### 1.4 更远的目标

通过这次把报告自动化跑通，**把整套研究方法沉淀成可迁移的 skill**
（迁移到 CATL 其他业务线、其他品类／行业／公司），
并**记录每一次迭代思考（含纠错反复）作为训练个人数字分身的语料**——
这就是 `DECISIONS.md` 存在的第三个理由。

---

## 二、方法论主线（先读这条，它决定其他一切）

**详见 [DECISIONS.md](./DECISIONS.md)「2026-09-02 · 方法论主线」。三条纪律：**

**① 精度不等于假设数量。**
> 毛估估 vs 算细账，区别不在于程序算多少步，而在于**你要拍多少个数**。

电池价格路径由 5 个参数生成 2026–2045 每年的价格——程序算 20 个数，你只拍了 5 个。
**逐年算电池新增假设数 = 0，所以它不是细账。**
判据：**这一步要不要我新拍一个数？要 → 拒绝。不要 → 算，免费的。**

**② 让程序算细，让报告读粗。**
程序逐年逐池逐代地算；报告只读终局年的几个数。中间明细是**可下钻的证据**，不是要读的内容。

**③ 标定一次，用很多次。**
精算一次标定误差，之后放心用粗口径，参数大改才重标。
（本轮实测：把全部批次按一个标准年处理，代价仅 **0.9%**，而服务费的不确定度是 ±33%。）

### 分层结构（定稿）

| 层 | 算什么 | 粒度 | 口径 | 谁读 |
|---|---|---|---|---|
| **L1 单位** | 每个 (站型 × 装机年) 的全周期流，按代展开 | 58 条 cohort | 单位 EAC、单代折旧 | 程序 / 树下钻 |
| **L2 站型** | 4 个站型的代表值（**2026 标准年**） | 4 个 | 覆盖倍数 | **报告读这一层** |
| **L3 集团·门槛** | 加权平均覆盖倍数 | 1 个 | **无量纲比值** | 第 3 章可行性 |
| **L4 集团·估值** | PV@2026 资本 ＋ 终局 EBITDA | 2 个 | 带日期折现 | 第 5 章 |

**门槛检验用无量纲比值（免疫年份问题），估值才做折现。两条线不共用一个数。**
标准年取 **2026**（＝决策时点、＝估值基年，且方向保守），不取 2028——
**当误差小到无关紧要时，选择是口径问题不是数值问题：参照系一致 > 误差最小。**

### 三个资本数，三个用途，永不共用

```
门槛 lifecycle_capital_base   4,297.3   各批次锚自身 t=0 的 PV 之和 → ×CRF → 覆盖倍数
估值 valuation_capital_pv     3,725.0   全部 CAPEX 折到 2026        → NPV、股权价值
名义 nominal_total_capex      5,605.1   不折现实际净支出             → 交叉验证
```

---

## 三、已定的决策（不要重开）

> **本节是「一行结论 ＋ 索引」，不是论证。** 完整论证住在 `narrative/topics/`，
> 判断的来龙去脉住在 `DECISIONS.md`。**同一条结论只能有一个 home，这里只放指针。**
> （2026-09-06 瘦身：此前这一节把论证也写了进来，与 `框架提案.md` 大面积重复；
> 而两份文件变化速度不同——快的那份一定先漂。见「2026-09-06g」。）

### 3.1 业务判断

| 议题 | 结论（一句） | 完整论证 |
|---|---|---|
| **必要性的论证结构** | **防守反击**，不是竞争威胁。三条路里换电是唯一**既开源、又落在存量盘上**的那条。**不造"对手做成了"的分支** | `topics/必要性_三条路为什么是换电` |
| **2028 的紧迫性** | 站网必须在竞争死线前建成，是竞争格局倒逼的硬约束，属第 2 章窗口论证 | 同上 |
| **窗口成本不量化** | 标准型资产的窗口成本是**二元的**，不是连续量；只做定性论证，不进模型 | 同上 · 见 4.4 |
| **回购对照的位置** | 从必要性挪到第 3B 章**资金门**做财务纪律检验 | 同上 |
| **业务本质与承重方** | 七步拆解链；**第 ⑥ 步必须问出承重方**，且承重方要**同时控制成本与寿命两端**——只有电池生产者两端都在手里 | `topics/业务本质_第一性原理拆解` ·「2026-09-05e」「2026-09-05j」 |
| **目标市场的定义方式** | **由痛点强度定义，不由保有量定义**；站数一律由需求反推，不采用宣发口径规划数 | `topics/目标用户_谁的痛点` ·「2026-09-05g」 |
| **TCO 口径** | **整套引用 JPM，不各取一项**；边际对手是 **LNG 不是柴油**；载重损失必须计入 | `口径/TCO口径_JPM整套引用` ·「2026-09-05i」「2026-09-05k」 |
| **竞争格局：三个战场** | ①路线之争（华为是主要对手，**被动逼近**；比亚迪封闭、不抢车企但砍天花板）②合作方之争（**两种模式的根因是议价能力不是聪明程度**；CATL 面对强议价方，**开放性即脆弱性**）③赛道内之争（**已无够格独立对手，且是结构性的**）。**不给概率，给"一定不成的条件＋逼近度"** | `topics/竞争格局_三足鼎立与赛道内玩家`<br>考古记录见 `audit/竞争格局_信源与矛盾清单` |
| **产能核查是可行性的一道必查项** | **终局年目标装机（制造 ＋ 换电运营）必须远小于现有产能 ＋ 规划产能**，而且要给储能、AIDC 及其他场景的电池需求**留出余量**。归第 3B 章**物理门**下的一项。**补产能信源时必须标注对应指标的时点** |
| **用人信号：最难伪装的承诺证据** | 换电条线负责人从**内部培养（技术＋公司内部）**换成**空降（运营＋产品＋资本）**，随后动作形态从"发布与宣告"变成"结盟与合资"。**换的不是人，是能力模型**——这是"这门生意是模式转变、不是技术变革"的第三方验证。**但它仍是"推"不是"拉"**，只能证明决心、不能证明标准已立；且是**滞后指标** | `topics/用人信号_战略定力与执行跟踪`<br>原始记录 `定性分析/用人信号.md`（持续更新） |
| **最大地主型合作方：独占假设从来不成立** | 该合作方在与 CATL 合作**之前**已建成超万座超充/快充站（总加油站约三万座）。它同时接入三种生态，**目标函数是"保住站址在电动化时代的价值"，凡有利于此的动作它都会做**。所以风险形态**不是二元的"退出"，是连续的"换电专属性下降"**；对"必须深度绑定"那条论证是**加强**——它结构上就不可能排他 | `topics/竞争格局_三足鼎立与赛道内玩家` 第六·补 |
| **飞轮五层是业务侧的跟踪骨架** | 车辆规模 × 货币化能力 × 电池周转率 × 资本成本 × CATL 权益，**任一层塌掉飞轮就停转**；**最该高频盯 ①②**（都有公开价格信号，不必等财报）。**飞轮跟"生意跑没跑通"，中检点跟"标准立没立住"，两套指标不能互相替代** | `topics/赔率与跟踪_从世界A到世界B` |
| **并购一定会发生，它改的是节奏** | 不是风险，是**预期中的事件**。三个维度：横向（买时间）／纵向（竞合，买成本与节奏确定性）／生态（买议价能力）。**已发生的那一笔：「结果」归格局、「打法」归并购。** 进模型的量是**网络加速多少年**与**承担多少负债**，是情景轴的 driver。**买什么说明缺什么** | `topics/并购_节奏与竞合` |
| **三段刻度与硬止损** | 持有期不是无差别等待：**中检**看不到＝需求侧假设错、规模下修但论点还在；**决断**看不到＝**论点被证伪，该走，与价格无关**；**兑现**看不到＝相变没发生，收益只剩业绩那一半。<br>⚠️ **"止损与价格无关"和 3.2「倍数是一个声明」里那条"市场不给 SOTP 不构成改倍数"是同一条纪律的两半**：**价格不给不是证伪，业务事实不兑现才是。** 两处必须一起读 | 同上 |
| **运营期限 vs 终局年** | **两件事。** 终局年＝我什么时候来看；运营期限＝这门生意能活多久，由资产性质决定。查证为**永续经营型** | `topics/运营期限_换电是哪类资产` ·「2026-09-05f」 |
| **测算期 / 终局年** | 2026–2030。理由＝**竞争死线 ＋ 两年爬坡**；十五五口径是佐证不是主因；**REITs-ready 是能力不是目的** | `topics/终局年_为什么是2030` |

### 3.2 非共识与估值

| 议题 | 结论（一句） | 完整论证 |
|---|---|---|
| **非共识的类型判定** | 断档不是机会，**断档接不上的原因才是**：A 相变型／B 线性型／C 我看错了。**本案判为 A 型**，C 必须显式排除 | `topics/非共识_类型判定与证据形态` ·「2026-09-05p」 |
| **共识锚** | **取形式证据不取数值**：卖方用不用 SOTP／换电有没有被单独列示／有没有机构给过独立估值。**三个"否"＝ 当免费期权。不做 PE 反推** | 同上 |
| **估值主口径** | **先定估值方式，再定参数**：换电→EBITDA×EV/EBITDA，制造侧→PE，整体 **SOTP**。**换基数是换量纲**，所以「ΔE × Δ倍数」不成立。第 5 章按四步写 | `topics/估值_为什么拍这个倍数` · `框架提案.md` §1.5 |
| **倍数的论证方式** | **三族定位 ＋ 性质判定**，不用可比公司取平均。判据是"谁扛资产、谁赚什么钱、赚的钱落在哪盘生意" | 同上 ·「2026-09-05e」 |
| **倍数是怎么来的** | **两条相反方向的边界在同一点相遇**（基建族上限＋战略溢价 ／ 资管族下沿），**不是拍中数**。报告必须写出这个收敛 | 同上 ·「2026-09-06c」<br>⚠️ **动笔前先读 `v3.2 §4.1`** |
| **倍数是一个「声明」** | 不是测算结果，**这完全正当**。义务是三件事：内容／位置依据／**什么会让我改它**——含**"市场不给 SOTP 不构成改"** | 同上 |
| **不设独立的「标准价值」科目** | **撤销**。商誉是残差，未实现的溢价无法倒算；**倍数本来就是装未来的**，错的是不说什么会改它 | 同上 ·「2026-09-06a」 |
| **期限只有两端** | 设备到寿不再更新（有限期账）／立住则不结束（永续账）。**两本账就是这个二分，不做中间档敏感度** | 同上 |
| **世界 A／B 的可观察边界** | **补能 vs 用能**——判据是它出现在用户的**哪个决策**里（用车时的服务选择 vs **购车时的选型条件**） | `topics/非共识_类型判定与证据形态` ·「2026-09-06b」 |
| **对标：已被接受的重估先例** | **特斯拉卖车→卖里程**。**借机制不借数值**；作用是把举证责任从"这种事可能吗"移到"这一次像不像" | 同上 |
| **先行指标必须标推/拉** | **只有"拉"算数**。中检点第一条是**规模档位**，但**被假设出来的规模不算证据，被观察到的才算** | 同上 |
| **资本闭环：三层要分清** | **现金流层**——出表不创造市值魔法（一次性回笼＋经常性收费的现值 ≈ 继续持有）；**规模层**——标准确立→载体可换→**可管理规模扩张，管理费基数变大**（属桶②(ii)，不是现金流加项）；**倍数层**——出表能力被证明改变**族定位**，族定位才决定倍数区间。**三层混一层就是重复计价。** 同业已有 ABS／类 REITs 先例且发行利率创新低，不是设想。**结论：REITs 跑通 ＋ 拓展窗口到来，两个条件同时成立时，现在这个倍数偏保守**（缺一不可） | `topics/资本闭环_REITs与轻资产化`<br>量化与外部锚见 `口径/capex_debt_估值公式链` §5 |
| **增量价值看合并净影响** | 换电的价值 ＝ **运营单体 ＋ 对制造的影响**，两块各用各的倍数。**虹吸与锁量不是重复计提**，是先减后加的净额，合并抵消已单独处理。**两把标尺**：相对"不做换电"是小幅正增量；**相对"现状"——无换电会塌，有换电三情景皆正**，后者才是决策该看的 | `topics/增量价值_运营单体加制造影响` |
| **倍数论证的材料边界** | 材料已齐，剩下是整理不是研究。**禁止引用启源芯动力交易做倍数依据** | `v3.2 §4.1`＋「2026-09-05e」「05f」「05j」「06c」 |
| **第 4 章的形态** | **终局年并表贡献 ＋ 一句节奏说明**，不做逐年表 | 见 4.4 |

### 3.3 工程与治理

| 议题 | 结论（一句） |
|---|---|
| **报告形态** | 一份长报告，章节可折叠、树可下钻。**不分册** |
| **HTML 的边界** | 只做四件事：加载 MD、替换占位符、画图、挂沙盘。**不许出现任何业务数字、公式、口径判断** |
| **六问包住八章** | 报告外面包一层读者视角的六问（为什么必须做／做多大／花多少钱／换回什么／钱够不够／**值不值这个价**），**开头给一句话答案＋指向章节，结尾同样六问、答"我可能错在哪"** |
| **叙述层两层结构** | `chapters/` 八章固定 ＋ `topics/` 按三判据长；**先写专题，再写章**；文件名必须 `.src.md` |
| **文件治理** | 状态／过程／产物三分；版本号只长在发布产物上；留痕用 git 不用复制目录 |
| **沉淀形态** | 三个 skill（非共识标的诊断／反事实定量建模／研报装配）＋ 一个「反方审稿」subagent。**不做九个 agent**，等跑通了再写 |

---

## 四、当前状态

### 4.1 已完成

1. **叙述层封口**（`facts.py` / `inject.py` / `build.py` / `narrative/`）
   叙述层不许出现字面数字，只写 `{{token}}`；改参数重跑全文自动刷新；越阈值段落自动标"待复核"。
2. **链路审计与决策树**（`tree.py`，取代 `chain_table.py`）
   根是决策不是车辆；每个内部节点跑「父＝f(子)」自校验；每个叶参数必须有信源。
   166 节点、57 个可校验内部节点全闭合。支持 `--json` / `--xlsx`。
3. **参数信源补齐**：18× / CRF 0.15 / PE 20× / 350天 / 0.80 / RTE 0.92 / 站用电率 2%。
4. **辅助服务字段治理**：容量补偿 + max(需求响应,调频)，防止三项相加。
5. **折旧与资本口径**：按代展开——每代按自己那年的价格买入、折旧到自己实际能收回
   的数为止；`unit_vintage` 留存 58 条 cohort 的单位经济性；折旧会计恒等式按构造
   成立（容差 1e-6）：`累计折旧 == 毛支出 − 期中残值 − 期末残值`。
6. **capex/debt/DCF 估值口径**：debt 统一为稳态口径（2030年在役资产历史成本×债务
   比，cohort逐批算出，倍数法与DCF法共用同一个数）；DCF 按**两本账**计算——
   **有限期账**（真实更换排期精确到期折现、毛现金流资本化、电池期末残值，站体
   设备不设残值）与**永续账**（不设终点、可持续更新支出按更新理论第一性原理算、
   递减永续年金、站体设备每15年全额更新）；两本账均按四个业务池分别算完再加总。
   完整公式、通用方法论、当前数字见 `口径/capex_debt_估值公式链.md`——这条链的过程/
   纠错记录全部在 `DECISIONS.md`，本文件不重复。
7. `python src/run.py` 与 `python src/tree.py`（三档情景）均通过。

### 4.2 当前关键数（中枢情景）

**规模（2030年，100%项目口径）**：

| 指标 | 数值 |
|---|---:|
| EBITDA | 850.4 亿 |
| EBIT | 508.0 亿 |
| 折旧 | 342.4 亿 |
| FCFF（毛，不扣任何资本性支出） | 723.4 亿 |
| 项目净利 | 351.6 亿 |
| CATL归属净利（40%持股） | 140.6 亿 |

**投入（@2026现值，100%项目口径）**：

| 指标 | 数值 | 用在哪 |
|---|---:|---|
| 纯初装现值（仅2026-2030建设，不含未来更换） | 2,281.9 亿 | 永续账"投入"侧 |
| 估值资本PV（含2026-2045全部真实更换排期） | 3,725.0 亿 | 有限期账"投入"侧 |
| 站体设备总投入（名义） | 333.6 亿 | 两本账共用 |
| 稳态debt（2030年在役资产历史成本×60%） | 1,565.9 亿 | 两本账+倍数法通用 |
| CATL权益出资（仅初装口径，×40%股权比×40%债务后自有资金比） | 365.1 亿 | |

**估值输出**：

| | EV | NPV@7.5% | CATL 40%归属 | 隐含倍数(vs EBITDA) |
|---|---:|---:|---:|---:|
| 倍数法（拍定18×） | 15,307.5 亿 | — | 5,496.6 亿 | 18×（拍定） |
| DCF有限期账 | 6,385.7 亿 | +1,650.9 亿 | 2,023.9 亿 | 7.51× |
| **DCF永续账** | **6,787.7 亿** | **+3,740.3 亿** | **2,088.7 亿** | **7.98×** |

押注部分（倍数法归属 − DCF永续账归属）：3,407.9 亿——这是需要在报告第5章用
成长性/期权/低风险这几个理由正面论证的真实溢价，不是可忽略的差额。

三档情景（保守/中枢/激进）DCF永续账CATL归属：1,893.1 ／ 2,088.7 ／ 2,293.8 亿。

### 4.3 未做

- **`base.toml` 情景元数据 ＋ `run.py` 整体重跑**（下一步，见 5.1）
- **`facts.json` 增设 `ext.*` 命名空间**（外部引用事实：可比倍数、JPM TCO 结论、电池价格）。
  `facts.py` 只生成模型算出来的数，但**叙述层的裸数字纪律同样管着外部引用事实**。
  每条必须带 `from`（信源）与 `as_of`（抓取日期）。
  **待补清单已列好、逐条挂了信源**：`audit/清场检查_0a-0c.md`。
  现在 `topics/` 里的 17 个 `{{ext.*}}` 占位符全是未定义的，`build.py` 会报"未知占位符"——**那是对的**，
  它正好是这份清单的自动校验。
- **口径文档的数值占位符化**（本轮立了规矩，未实施）：口径文档里的数字分三类——
  ①输入参数写 `base.toml` 键名不写值／②计算结果写占位符／③口径本身的取值写数字。
  **机制**：口径文档改 `.src.md`，**`inject.py` 的扫描根从 `narrative/` 一个扩到 `narrative/` ＋ `口径/` 两个**，
  产出 `outputs/口径_*.md` 可读版。**要读读产物，要改改源文件，与 `narrative/` 同构。**
  ⏳ 卡在 ②类需要的 `facts` 键大部分还不存在（与 `ext.*` 同一个缺口），**待 facts 扩容后一次性做**。
- ✅ **`facts.py` 已支持 `ext.*`**（2026-09-06 落地）：新增 `ExtFact` 类 ＋ 16 条外部引用事实，
  **source 与 as_of 缺任一直接报错**。`ext.catl_equity_share_blended` 撤销——
  它是**派生量**不是外部事实，改为 `base.catl_blended_share`（＝(1−债务比例)×建站持股比例）。
  **`build.py` 现在应能跑通全部 13 份专题。**
- **两条待带程序核对**（口径迁移时裁决留下的）：
  ① **全网站数两个值**——根源是四池后分，需跑 `build.py` 看实现取的是哪个；
  ② ~~站侧人工是否仍按次计~~ ✅ **已核查关闭**：`src/` 零命中，`base.toml` 只有一行注释记载废弃。
  **实现里没有按次计价，纯属文字未同步。**
- ✅ **`MANIFEST.md` 的接管已完成**：§2/§3 → `口径/车辆与站数_推算方法.md`、
  §4 → `口径/运营收入与成本_口径.md`（两份本轮已建）；§6.2/6.3 与 §8.3 已由两份专题接管。
  **但归档仍需你确认**——迁移时发现原文有 **12 处一量两值／自相矛盾**，逐条列在两份口径文档的
  「⚠️ 迁移时发现的问题」一节里（**未裁决**），其中站侧人工"按次计"那条建议回查 `src/` 与 `base.toml`。
- **竞争格局那六份旧稿的归档也要等**：条件是 **① 专题定稿 ② 它支撑的报告章节被你确认**。
  理由见 `audit/竞争格局_信源与矛盾清单.md` 第七节——
  **覆盖检查回答"能不能接管"，报告验收回答"接住了没有"；后者只有人能做。**
- **`{{topic: ...}}` 引用机制不存在**。`框架提案.md` §1.4 写了章里可以这样引专题，
  但 `inject.py` 的 `TOKEN_RE` 只认 ASCII 键名、冒号后是格式说明符。**归入第 8 步（装配）**，
  现在章里用普通链接引专题即可。
- **痛点强度计算**（第 3A 章收口）：TCO 参数**整套采用 JPM Table 3**
  （数据与口径已搬到 `口径/TCO口径_JPM整套引用.md`）、`scale.py` 加分车型 TCO 对比与敏感度分级。
  三条必须带进去：**① 真正的边际对手是 LNG 不是柴油**（3年 −4% vs −17%）；
  **② 载重损失 7 万元/年必须计入**；**③ 补贴退坡（2026/2027 到期）进情景轴**
- **七份专题** `narrative/topics/`（清单见 `框架提案.md` 1.4 节）——**它们是章的地基，先于章**
- 四道门槛清单（物理／经济／资金／被快充替代）结构化输出
- 树打通到底：车辆漏斗、站数反推、电池寿命与 590 折扣链、制造侧锁量与虹吸
- 八章正文、HTML 装配、交互沙盘、三个 skill
- `dcf_legacy_debt_yi` / `dcf_ev_at_crf_yi` / `dcf_ev_at_wacc_yi` 是对照字段，
  待第 5 章正文定稿、确认不再需要旧口径交叉核对后统一删除
- **`ledger` 里「充电份额流失 −1,153」是否与「锁量 +1,075」重复计提，口径待查**
- `outputs/换电战略决策报告_v4.3.md` 与 `换电投资逻辑_叙述报告_v4.3.md` 里
  叙述引用的数字，需跑一遍 `python src/build.py` 重新核对±3%待复核阈值（这条链
  的口径这一轮变动很大，之前跑出的"9段待复核"清单已过期，需要重新生成）——
  第 5 章正文开写前必须先看一遍，不急，但不能漏


### 4.4 已取消（2026-09-05e 定，不要再捡回来）

| 曾经的待办 | 取消理由 | 想恢复的条件 |
|---|---|---|
| **年度化逐年并表贡献 2026–2030** | 决策是**问题 A**（2030 终局判断），逐年并表 EPS 服务不了任何一个已定的决策问题：判断"什么时候看得到"一句话够了，共识锚对的是终值不是逐年，中检点要的是**里程碑**（站数/渗透/区域盈利）不是并表 EPS。第 4 章改为**终局年并表贡献 ＋ 一句体现节奏的定性说明**。连带取消 `construction.ramp_years` 参数 | 决策问题从 A 改成 B（要"今天这一刻公允价值"） |
| **窗口成本（排期后推 ＋ 下调渗透率上限）** | ① 要新拍**至少两个无信源的数**（后推几年、上限下调多少），而这两个数**共同决定结论**——等于结论完全由假设决定，违反方法论主线"要新拍数 → 拒绝"；② 更根本：**标准型资产的窗口成本是二元的**（位置被占了 / 没被占），不是连续量。**把二元风险算成连续量，等于把"可能全输"表达成"少赚一点"，是系统性低估风险**。改为第 2 章一句定性论证 | 若判定换电只是**场景拓展**（非标准确立），窗口成本才重新变成连续量，那时可以算 |
| **T1（18× 倍数是否合理）＋ 引用启源芯动力交易** | ① "T1"是一个**在别处失传的编号**，违反可独立阅读纪律；② **倍数论证的材料已经够了**——`分析结论/换电毛估估_折扣链与总表_v3.2.md §4.1` ＋ `DECISIONS.md`「2026-09-05e」「2026-09-05f」「2026-09-05j」三条，**剩下的是写报告专题，不是再研究**；③ **启源芯动力交易不能做倍数依据**——CATL 对国资的并购有多种考量，不是市场化定价（`MANIFEST.md §8.2 参照公司卡片`已引述） | 出现**市场化的、可比的**换电资产成交价 |

---

## 五、下一步任务清单（按顺序）

### 5.0 清场（写正文之前必须做完，否则新对话会被旧文件误导）

这四件都不难，但**必须在有人读到那些旧文件并当真之前做完**。

| # | 做什么 | **谁做** | 你要执行的具体动作 | 验收 |
|---|---|---|---|---|
| **0a** | **`MANIFEST.md` 覆盖检查** | ~~AI~~ **✅ 已完成** → **你 `git mv`** | 报告：`audit/清场检查_0a-0c.md`。**结论：没有一节是"没人接"的，可以归档**；唯一要处理的是 §8.2 参照公司卡片的引用改指 `_archive/` | 报告已出；等你执行 `git mv` |
| **0b** | **`templates/` 引用检查** | **✅ 已完成（AI）** | **结论：仍在被引用，不能归档**——`src/report.py:2635` 用它生成战略决策报告。**`templates/` 保留**，已补准入判据 | 原假设"若 src 已不引用"**不成立**，已从搬迁清单移除 |
| **0c** | **`narrative/` 旧稿判定** | ~~AI~~ **✅ 已完成** → **你 `git mv`** | **判定：是旧的 Q1–Q4 模型说明书骨架，不是新八章骨架，归档**。归档前已摘出两处不该丢的东西（见报告） | 连带发现并已修：`inject.py` 的 glob 只扫顶层，搬迁后会**静默**扫不到任何文件 |
| **0d** | **`outputs/` 重跑** | **你** | `python src/build.py`（AI 侧的本地 shell 目前不可用，你机器上跑最快）；把"待复核"清单贴给 AI | 生成物是两本账口径；新待复核清单已产出 |
| **0e** | **搬迁 ＋ 改路径引用** | **你** | 见下方命令块 | `git status` 干净，全文搜索无失效路径 |

**0e 可直接执行的命令**（在 `换电战略决策模型_v4_3/` 下）：

```bash
git mv capex_debt_估值公式链.md 口径/          # 口径/ 目录 AI 已建（放了 TCO 口径文档）
git mv MANIFEST_original.md _archive/
git mv MANIFEST.md _archive/                                    # 0a 已确认
git mv narrative/换电投资逻辑_叙述报告_v4.3.src.md _archive/       # 0c 已确认
# templates/ 不动 —— 0b 查出 src/report.py 仍在引用

grep -rn "capex_debt_估值公式链" --include="*.md" .   # 检查残留的旧路径引用
git add . && git commit -m "清场：MANIFEST 与旧叙述稿归档、口径目录建立 · 见 DECISIONS 2026-09-06g"
```

> **0d 之后请把"待复核"清单贴回来。** 预期这一跑会报**未知占位符**——
> `topics/` 里有 17 个 `{{ext.*}}` 还没定义，那是 4.3 里那条待办的自动校验，不是故障。

### 5.1 base.toml 情景元数据 ＋ run.py 整体重跑（清场之后）

现状：`run.py` 只在 `private_scenario` 上重跑，只作用于私家车渗透率一处；
三情景归属价值 4,656 / 5,092 / 5,565（±9%）——**那是私家车的区间，不是投资风险区间**。
根因：base.toml 里情景信息有**五种互不相通的写法**，其中一种只写在注释里、程序中不存在。

改法：
- 每个参数补 `role`（情景轴 / 不变量 / 待核实）；情景轴必须带 `driver` ＋ 三档值
- 文件顶部新增 `[drivers]` 段，声明 **5–10 个驱动因子**，每个配一段人话叙事
  （这段叙事直接作为第 0 章决策卡与第 7 章诚实清单的素材）
- **`run.py` 改为按 driver 组合整体重跑**——要么全打包按情景跑，要么全跑中性，**不许散装**
- `tree.py` 读 `role` 打徽标，并输出「悲观→乐观区间中各驱动因子各贡献多少」的归因分解
- **四道门必须三情景都判**：覆盖倍数 1.14× 的余量，在悲观情景下会不会翻，是第 3 章最关键的一个事实

只让**同源驱动**的参数联动——让 212 个参数一起变好变坏 = 假设全部完全正相关，区间会宽到没有意义。

### 5.2 之后

2. **四道门槛清单**结构化输出：每道门给通过与否、余量、翻转阈值。回购对照并进资金门。
3. **树打通到底**，每个叶子挂信源链接与 `lab.py scan` 实测弹性。
4. ~~**写专题**~~ **✅ 八份已写**（`narrative/topics/`，lint 全部通过）。
   剩下的是**回填 `{{ext.*}}`**（见 4.3）与人 review 论证本身。
   其中「估值_为什么拍这个倍数」的材料已齐（v3.2 §4.1 ＋ DECISIONS 三条），**是整理不是研究**；
   「非共识_类型判定与证据形态」的材料也已齐（`框架提案.md` §1.6 ＋ DECISIONS「09-05p」）。
5. **写八章正文**，逐章交付；全部占位符。外部取数一处：**共识锚——取形式证据不取数值**
   （翻研报核对三条：卖方用不用 SOTP／换电有没有被单独列示／有没有机构给过独立估值，
   逐条标来源与日期。**不做 PE × EPS 反推**，理由见 `框架提案.md` §1.6）。
   可比倍数阶梯从 v3.2 §4.1 整理，**不引用启源交易**。
6. **装配＋沙盘**：薄 HTML；先弹性外推给即时反馈，Pyodide 加载完切精确模式。
7. **沉淀三个 skill ＋ 项目约定**。

---

## 五·补 纪律登记表（2026-09-06 新增）

> 每条纪律登记三件事：**约束谁 / 什么时候检查 / 怎么验证执行了**。
> 缘由见 `DECISIONS.md`「09-06d」——同一族失败（规矩对、投放位置错）已连续四次，
> 根因是**纪律散落，只能靠记忆调用**。规则本身见 `研究项目约定.md` §4 第六条。

| 纪律 | 约束谁 | **什么时候检查** | 怎么验证执行了 |
|---|---|---|---|
| **读 `v3.2 §4.1` 再谈倍数** | 第 5 章、「估值_为什么拍这个倍数」专题 | **动笔前** | 文中出现"两条边界双向收敛"的表述，而不是"14 上面加溢价" |
| **五行表逐行走** | 每一轮协作 | **每轮结束前** | 五行逐行答"有／无"，有则当轮落盘 |
| **专有名词不许失传** | 五份纲领文件（README／约定／框架提案／交接／DECISIONS） | 每次编辑 | 全文无未定义的编号与缩写 |
| **二元不许算成连续** | 运营期限、窗口成本、"标准立没立住"这类量 | **每次要建敏感度表时** | 敏感度表里没有中间档；只有两端 |
| **先行指标标推/拉** | 第 5 章第 3 步、第 6 章中检点 | 每次新增指标 | 每条指标后面都有「推」或「拉」 |
| **改前先取正本** | 全部仓库文件 | **每次动手改之前** | 本轮取版本的时间戳有记录，写回时带上了 |
| **日志由 AI 主动起草、用人的视角** | `工作记录/研究历史记录.md` | **每轮结束前**（不等人开口） | 小节标题是结论不是事件；AI 以第三人称出现；人在草稿上改而不是从零写 |
| **叙述层不许有裸数字** | `narrative/**` | `build.py` 装配时 | 正则扫描通过（年份等白名单除外） |

---

## 六、协作协议（2026-09-06 修订）

**核心：改之前先说清楚要改什么、为什么、影响哪些文件——让我能跟上思路，
有问题当场纠正，避免无谓的工作。**

**四步：**

| 步 | 谁 | 做什么 |
|---|---|---|
| **1** | **AI** | **先说明分析与方案**：改什么、为什么、影响哪些文件、有哪些待拍板项。**这一步不许跳** |
| 2 | 你 | 说「过 / 停 / 改」，或先答待拍板项 |
| 3 | AI | 改，**重跑通过**才算数；落盘后说明改了哪几个文件、每个文件改了什么 |
| 4 | 你 | 在 IDE 里 `git diff` 看具体行，`git commit -m "<一句话> · 见 DECISIONS <日期>"` |

> **2026-09-06 变更**：取消"贴 unified diff / 存 `CHANGES_*.diff`"这两步——
> 你在 IDE 里用 git 就能逐行跟踪，AI 再贴一遍是重复劳动。
> **review 的闸门从"看 diff"前移到"看方案"**：更早、更省事，而且拦得住的是
> 方向性错误（diff 只能拦实现错误）。

**三条硬约束（保留）：**

1. **一次交付只改一个关注点。** 一次改动跨越太多文件就拆两次。
2. **每个改动必须配一条 DECISIONS 条目。** git 答"改了什么"，DECISIONS 答"为什么"。
3. **改动点必须在代码里留注释**，写明改了什么口径、为什么、指向哪条 DECISIONS。

**第四条（2026-09-05e 新增）· 讨论不留在对话里。**
一轮讨论如果产生了有价值的判断，**在同一轮就落进文件**，不等"下次整理"：

| 产生了什么 | 当轮落到哪 |
|---|---|
| 一个判断从 A 变成 B | `DECISIONS.md` 一条（带类型 ＋ `→skill:`） |
| 当天的思路、读到的东西、发散 | `工作记录/研究历史记录.md` 一段 |
| 目标态变了（要造成什么样） | `框架提案.md` |
| 当前态变了（跑到哪了、下一步） | 本文件 |
| 口径变了 | `口径/capex_debt_估值公式链.md` |

**每轮结束前 AI 自问一次：这轮讨论产生了什么该进文件的东西？** 没有则明说没有。

### git

一个目录只能有一个 `.git`，**不能嵌套**（嵌套时外层把内层整个当未跟踪对象，表现为编辑器一片绿点）。
诊断：`git status --short`（`U`=未跟踪 `M`=改过未提交 `A`=已加待提交，无输出=干净）。
`.gitignore` 至少包含：`__pycache__/`、`*.pyc`、`~$*`、`*.tmp`。

---

## 七、文件分工（状态／过程／产物）

| 类型 | 特征 | 本仓库里是谁 |
|---|---|---|
| **状态**（现在是什么） | 只有一份，永远最新，可覆盖 | **本文件**（当前态）、**`框架提案.md`**（目标态）、**`研究项目约定.md`**（工作台约定）、`README.md`（导航）、`口径/`、`configs/`、`src/`、`narrative/chapters/`＋`narrative/topics/`、`audit/` |
| **过程**（怎么变成这样） | 只追加，永不修改条目 | **`DECISIONS.md`**（按主题·六栏·带类型与 `→skill:` 标记）<br>**`工作记录/研究历史记录.md`**（按日期·日志体） |
| **产物**（算/生成出来的） | 可随时重跑，删了不心疼 | `outputs/` 全部、`CHANGES_*.diff` |

**判据（最容易判错的是"产物"）：能不能从「状态」重新生成？**
能 → 产物；不能 → 它其实是状态，只是伪装成产物。**孤儿文件全部来自这里。**

**版本号只长在发布产物上**（`outputs/..._20260902.html`），不长在源目录上。
**「只追加」约束的是 DECISIONS 的条目正文，不约束它的索引表**——索引是导航件，必须始终完整。

### 状态层内部再分：目标态 vs 当前态

| | 回答什么 | 变化速度 |
|---|---|---|
| `框架提案.md`（**目标态**） | 要造成什么样、怎么造、按什么顺序造 | 很慢——只在框架决策变化时改 |
| **本文件**（**当前态**） | 现在跑到哪了、下一步干什么 | 每轮都改 |

**目标态文档里不许出现当前值**——需要提到某个量就写字段名和住址。
判据：改一个参数重跑，那份文件需不需要改？需要 → 写错地方了。

### 两份过程文件的分工

| | `研究历史记录.md` | `DECISIONS.md` |
|---|---|---|
| 体裁 | 日志，按日期 | 条目，按主题，六栏 |
| 记什么 | 当天在想什么、读了什么、发散到哪 | **一个判断从 A 变成 B，以及为什么** |
| 时机 | 每次工作后 | 每次一个结论被推翻或定下 |
| 谁读 | 你自己回溯思路 | 下一轮接手／数字分身语料／**报告第 7A 章** |

**`DECISIONS.md` 记的是所有研究转折，不只代码**，四类：
`研究判断`（最值钱）／`建模口径`／`方法论`／`工作方式`。

---

## 七·补、目录约定与待搬迁（2026-09-05e 定）

**通用约定见 [`研究项目约定.md`](./研究项目约定.md) 第 2 节**（根目录五份纲领、
每个子目录一句准入判据、新建文件三问、章数固定专题按判据长、归档先做覆盖检查）。
下面只写**本项目的落地状态**。

### 目标结构

```
换电战略决策模型_v4_3/
├── README.md              导航
├── 研究项目约定.md          工作台约定
├── 框架提案.md              目标态
├── 交接.md                  当前态（本文件）
├── DECISIONS.md            过程·按主题
│                           【根目录只放这 5 份】
├── configs/               参数
├── src/                   程序·唯一引擎
├── 口径/                   跨环节口径与方法论证   ← 已建
├── templates/             输出格式模板（`src/report.py` 在用，**不归档**）
├── narrative/
│   ├── chapters/          八章（数量固定）
│   └── topics/            论证专题（按判据长）
├── audit/                 台账
├── outputs/               生成物
└── _archive/              已被取代的
```

> 本项目之外、属于同一研究的：`业务/换电/工作记录/研究历史记录.md`（过程·按日期）、
> `业务/换电/media/`（外部报告原文，如 JPM）。**不搬进来**，此处给指针即可。

### 待搬迁（在 IDE 里 `git mv`，比来回传文件快）

| 文件 | 现在 | 该去 | 备注 |
|---|---|---|---|
| `口径/capex_debt_估值公式链.md` | 根目录 | `口径/` | **可直接搬**，搬完把 `交接.md`／`框架提案.md`／`README.md` 里的路径引用改掉 |
| `MANIFEST_original.md` | 根目录 | `_archive/` | **可直接搬**——职责已被 `MANIFEST.md` 接管 |
| `MANIFEST.md` | 根目录 | 覆盖检查后 → `_archive/` | **先做任务 0a**，不可直接搬 |
| ~~`templates/`~~ | 顶层目录 | **不搬** | **0b 已查：`src/report.py:2635` 仍在引用**。保留，并补了准入判据 |
| `narrative/换电投资逻辑_叙述报告_v4.3.src.md` | `narrative/` | `_archive/` | **0c 已判：旧的 Q1–Q4 骨架，可归档**（归档前已摘出两处保留项） |

---

## 八、踩坑清单

### 口径类

- **折旧不是资本要求，资本要求也不是折旧。** 折旧只服务于**税**（决定税盾），
  EAC 只服务于**资本要求**（决定门槛）。共用一个分子必出事，而且
  **折旧虚增 → 利润表被低估（看起来亏钱）＋ 税盾虚增 → 估值被高估，两个方向相反、会互相掩盖。**
- **任何年金化／摊销，先问分子分母是不是同一段时间。**
  `(15年PV倍数) ÷ (单代寿命)` 看上去很自然，但它把 15 年要花的钱压进 3 年摊完。
- **换层级用同一个公式前，先问它的前提在新层级还成不成立。**
  EAC / IRR / 回收期 / 单位经济的前提都是"一笔投入对应一条现金流"，聚合到有建设期的资产组合就破了。
- **门槛类指标尽量做成无量纲比值**——比值天然免疫年份、规模、货币单位的口径问题。
- **取代表值时，参照系一致 > 误差最小**（标准年、基准情景、可比公司口径都适用）。
- **CRF 0.15 不是 WACC 口径。** WACC 7.5% 是成本线下限；CRF 用期望收益率
  （10–15% 中值，隐含约 12.4%），依据 v3.2 §1.2.2。用 WACC 反推的 0.1133 去质疑它是比错了对象。
- **18× 已含战略溢价，不应再叠加。** 但注意 v3.2 §4.1 说"锚 6.7–14× ＋ 温和溢价 → 18×"，
  而 **18 比区间上限 14 高 29%**，根本不在区间内——这是全篇最易被打穿处。
  再投资强度（EAC/EBITDA ≈ 76%）在基建族里属极高，应锚**下沿**。
- **辅助服务三项不能相加。** `ancillary = 容量补偿 + max(需求响应, 调频)`。直接相加会高约 29%。
- **`nev_rates` 是年更新流量口径，不是保有量口径。**
- **`outputs/` 下的 md 是生成物。** 改叙述正文要改 `narrative/*.src.md`。
- **一条"现金流÷折现率"公式里，如果投入侧已经用一份完整的"全部投资现值"去扣减，
  现金流侧就必须是毛值，不能再净一次"可持续资本性支出代理"——两边扣一次是对的，
  两边都扣是重复扣减。** 换一个更"讲究"的折现率不会自动修好这个问题，见
  `口径/capex_debt_估值公式链.md`第1.2节原则三、`DECISIONS.md`「2026-09-05c」。
- **持续更新型资产的"可持续资本性支出"，该用"存量规模÷更新周期"这个更新理论
  第一性原理算，不要挑某一年的实际数、也不要用会计折旧代理**（折旧混了历史买入价，
  且会漏掉资产本身的价格趋势）。见同上第1.2节原则四。

### 方法类

- **断言写成恒等式，不要写成不等式。** `≤` 型只能证伪不能证实。
  更进一步：**好的恒等式应当按构造成立**——把两边写成同一组数的两种排列，
  它就从"一致性检查"退化成"代码正确性检查"，更强、更便宜、更不会漏。
- **容差是用来吸收浮点误差的，不是用来吸收口径错误的。**
  容差 > 1e-6 量级时都要问：这个残差是数值误差，还是我还没查清的错？
  **说得出机制却说不出精确值，就是没查清。**
- **"跑不通"是信息，不是障碍。** 断言要卡在会失败的地方才有价值。
- **不要给一个数编解释。** 看到两个数接近就倒推因果，是最容易混进来的一类错误，因为它读起来最顺。
  **先验算，再解释。**
- **常识反推是有效的审计手段。**「折旧这么高就没人敢买充电重卡了」——比任何代码审查都先命中要害。
  **算出来的数与行业常识冲突时，先信常识。**
- **辨认时间窗要找「谁在逼你」，不是「你何时能兑现」。** 死线由外部竞争给定，兑现只是后果。
- **弹性 ≠ 情景。** 高弹性且高不确定 → 情景轴；高弹性但低不确定 → 去核实它；低弹性 → 不进情景。
  **谁是情景轴是一个「声明」，必须显式写在参数定义里**；`lab.py` 的弹性只用来审计这个声明。
- **散装补丁比没有补丁更危险**——"只给私家车做了三档"看起来像有情景分析，掩盖了"其实没有"。
- **口径分歧优先于数字分歧。** 三份文档数字不一致时，先看拆法是不是同一种。
- **不要为了一份注定被淘汰的产物做基础设施改造。** 先确认目标产物，再动手。
- **审计要分清"抓到了问题"和"对问题的推断"。** 前者是机械事实，后者是判断，不能混。
- **一个借来的假设参数（"N年寿命/周期"），先查它最初的出处回答的是哪个具体问题，
  再决定能不能用在当前这个问题上——两个问题字面像、实质不同是最容易出事的情形。**
  见`口径/capex_debt_估值公式链.md`第1.2节原则五（"15年"这个数最初回答的是"一批不再
  补充的存量电池能撑多久"，被借去回答"持续滚动更新的业务该追踪多久"，是两个
  不同的问题）。
- **两个独立算出的数字接近，不能直接当互相印证，先确认假设是否真的相同**——一个
  永续假设、一个有限期假设，算出接近的数纯属巧合，我自己在09-05c的追查过程里就
  犯过一次这个错误。
- **用户说不清楚具体错在哪、但反复觉得"不对劲"，是需要立刻升级追查力度的强信号，
  不是弱信号**——2.06%这个bug最终被挖出来，起点始终是这类说不出机制的持续怀疑，
  不是自查。见`DECISIONS.md`「2026-09-05c」。

---

## 九、新对话的开场（可直接粘贴）

> 继续宁德时代换电投资报告的自动化工作。
> 请先读 `D:\AI\证券投资\投研\能源\宁德时代\业务\换电\定量计算\换电战略决策模型_v4_3\` 下的
> `交接.md`、`DECISIONS.md`、`README.md`。
> `交接.md` 第三节的决策已反复讨论过，直接沿用不要重开；
> 第二节的方法论主线和第六节的协作协议**必须遵守**（改代码前先贴 diff 让我 review）。
> 从第五节任务清单 5.1「base.toml 情景元数据 ＋ run.py 整体重跑」开始。
